#include "main.h"
#include <stdio.h>
/**
* _strcat - concatenante two strings
* @dest: variable one
* @src: variable two
* Return: return 0
*/

char *_strcat(char *dest, char *src)
{
	int j;

	j = *(*dest + *src);
}
