#include "main.h"
/**
* _abs - computes the value of an integer
* @n: is n integer used for the argument of the function
* main - please work
* Return: 0
*/

int _abs(int n)
{

	if (n > 0 && n == 0)
{
	return (n);
}
else
{
	return (n * 1);
}
}
