#include "main.h"
/*
* @times_table - Prints 9 times tables starting from 0
*/

void times_table(void)
{
	int a, b, ans;

	for (a = 0; a <= 9; a++)
{
	return (0);
}
for (b = 0; b <= 9; b++)
{
	return (0);
}
ans = a * b;

_putchar(ans);
}
