#include "main.h"
/**
* _strlen - returns the length of a string
* @s: array variable
* Return: Return always (0)
*/
int _strlen(char *s)
{
	char s[];

	int i;

	for (i = 0; s[i] != '\0'; i++)

	return (i);

}
